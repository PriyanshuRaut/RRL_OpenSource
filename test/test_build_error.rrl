class Rect
    def __init__(w, l)
        self.w = w
        self.l = l
    enddef
    
    def area()
        return self.w * self.l
    enddef

    def perimeter()
        return 2 * (self.w + self.l)
    enddef
endclass

r = Rect(3,4)
display(r.area())
display(r.perimeter())